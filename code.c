#include<stdio.h>
#include<string.h>
void main()
{
  char p[10][5],temp[5];
  int i,j;
  float avgwt;
  printf("enter no of processes: ");
  for(i=0;i<n;i++)
  {
    printf("enter process%d name: ");
    sacnf("%s",&p[i]);
    printf("enter process time: ");
    scanf("%d",&pt[i]);
  }
  for(i=0;i<n-1;i++)
  {
    for(j=i+1;j<n;j++)
   {
      temp1=pr[i];
      pr[i]=pr[j];
